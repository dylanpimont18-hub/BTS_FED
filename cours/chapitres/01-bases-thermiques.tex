% ============================================================
%  Chapitre 01 — Bases de la thermique
%  BTS FED option GCF — Cours complet
%  Enrichi le 2026-04-03 — suppression section exercices
% ============================================================

\chapter{Bases de la thermique}

% ============================================================
\section{Objectifs pédagogiques}
% ============================================================

\subsection*{Compétences GCF mobilisées}

\begin{itemize}
  \item \textbf{C2-3} — Décrire le fonctionnement d'un système : identifier les paramètres
        thermiques (températures, puissances, débits).
  \item \textbf{C3-3} — Dimensionner tout ou partie d'une installation : appliquer les
        méthodes normalisées de calcul thermique.
  \item \textbf{C5-1 / C5-2} — Recueillir et extraire les exigences réglementaires (RE2020,
        DTU, NF EN ISO 6946).
  \item \textbf{C7-4 / C7-6} — Conduire des mesures et interpréter les résultats
        (thermomètres, fluxmètres, sonde de surface).
  \item \textbf{C8-3 / C8-4} — Analyser les performances énergétiques et concevoir des
        actions correctives.
\end{itemize}

\subsection*{Savoirs associés GCF}

\begin{itemize}
  \item \textbf{S4} — Sciences physiques : thermodynamique, états de la matière, échanges
        d'énergie.
  \item \textbf{S5.1} — Réglementation thermique RE2020 : exigences sur les parois opaques
        et le coefficient \(U_{bat}\). (Niveau GCF\,: 3)
  \item \textbf{A1-1} — Thermique des tubes : flux linéique et isolation. (Niveau GCF\,: 2)
  \item \textbf{A1-2} — Hygrothermie : condensation en paroi, dispositions constructives.
        (Niveau GCF\,: 3)
  \item \textbf{A2-3} — Bilan thermique : déperditions, charges thermiques et hydriques.
        (Niveau GCF\,: 3)
  \item \textbf{B1-2} — Émission de chaleur : radiateurs, planchers chauffants,
        ventilo-convecteurs. (Niveau GCF\,: 3)
  \item \textbf{S10.1} — Métrologie appliquée à la thermique. (Niveau GCF\,: 3)
\end{itemize}

% ============================================================
\section{Introduction}
% ============================================================

Le génie climatique et fluidique (GCF) a pour mission de concevoir, installer et maintenir les
installations de chauffage, ventilation, climatisation (CVC) et de production d'eau chaude
sanitaire (ECS). L'ensemble de ces systèmes repose sur un socle commun : la maîtrise des
\textbf{transferts thermiques}.

Un technicien supérieur GCF doit être capable de :
\begin{itemize}
  \item quantifier les pertes ou les apports de chaleur dans un bâtiment ;
  \item dimensionner les équipements de production et d'émission de chaleur ou de froid ;
  \item vérifier la conformité d'une installation vis-à-vis de la réglementation RE2020 ;
  \item diagnostiquer un dysfonctionnement thermique et proposer une action corrective.
\end{itemize}

La thermique du bâtiment est au cœur de la transition énergétique. En France, le secteur du
bâtiment représente environ \SI{45}{\percent} de la consommation finale d'énergie et
\SI{27}{\percent} des émissions de \(\mathrm{CO_2}\). La réglementation RE2020, en vigueur
depuis le 1\textsuperscript{er} janvier 2022, exige une réduction drastique des déperditions
thermiques et un recours accru aux énergies renouvelables.

% ============================================================
\section{Notions fondamentales de thermique}
% ============================================================

\subsection{Chaleur, température et énergie thermique}

\begin{definition}
  \textbf{Température} (\(T\)) : grandeur intensive caractérisant le niveau d'agitation
  thermique des molécules d'un système. Elle s'exprime en kelvin (\si{\kelvin}) dans le
  Système International (SI) ou en degré Celsius (\si{\degreeCelsius}) en pratique courante.
  Relation de conversion : \(T\,[\si{\kelvin}] = \theta\,[\si{\degreeCelsius}] + 273{,}15\).
\end{definition}

\begin{definition}
  \textbf{Chaleur} (\(Q\)) : forme d'énergie transférée entre deux systèmes due à une
  différence de température. S'exprime en joule (\si{\joule}) ou en kilowattheure
  (\si{\kilo\watt\hour}). Rappel : \(\SI{1}{\kilo\watt\hour} = \SI{3600}{\kilo\joule}\).
\end{definition}

\begin{definition}
  \textbf{Puissance thermique} (\(\dot{Q}\) ou \(P_{th}\)) : énergie thermique échangée
  par unité de temps. S'exprime en watt (\si{\watt}) ou en kilowatt (\si{\kilo\watt}).
  \[
    \dot{Q} = \frac{\mathrm{d}Q}{\mathrm{d}t}
  \]
\end{definition}

\begin{definition}
  \textbf{Capacité thermique massique} (\(c_p\)) : quantité d'énergie nécessaire pour élever
  de \SI{1}{\kelvin} la température d'un kilogramme de matière à pression constante.
  Unité : \si{\joule\per\kilogram\per\kelvin}. Valeurs courantes :
  \begin{itemize}
    \item Eau liquide : \(c_p \approx \SI{4186}{\joule\per\kilogram\per\kelvin}\)
    \item Air sec : \(c_p \approx \SI{1006}{\joule\per\kilogram\per\kelvin}\)
    \item Béton : \(c_p \approx \SI{880}{\joule\per\kilogram\per\kelvin}\)
    \item Acier : \(c_p \approx \SI{490}{\joule\per\kilogram\per\kelvin}\)
    \item Laine de verre : \(c_p \approx \SI{840}{\joule\per\kilogram\per\kelvin}\)
  \end{itemize}
\end{definition}

\subsection{Les trois modes de transfert de chaleur}

La chaleur se propage selon trois mécanismes distincts, souvent présents simultanément dans
une installation CVC :

\begin{itemize}
  \item \textbf{Conduction} : transfert au sein d'un milieu solide (ou fluide au repos) par
        contact direct entre molécules. Prépondérant dans les parois de bâtiment.
  \item \textbf{Convection} : transfert entre un fluide en mouvement et une paroi (ou
        inversement). Prépondérant dans les émetteurs de chaleur et les échangeurs.
  \item \textbf{Rayonnement} : transfert par ondes électromagnétiques, sans support matériel.
        Prépondérant pour les corps à haute température (brûleurs, surfaces chauffantes).
\end{itemize}

\begin{table}[H]
  \centering
  \caption{Comparaison des trois modes de transfert thermique}
  \begin{tabular}{p{2.8cm}p{4cm}p{4.5cm}p{3cm}}
    \toprule
    Mode & Mécanisme & Applications CVC & Formule clé \\
    \midrule
    Conduction & Vibrations moléculaires en milieu solide ou fluide au repos & Parois isolées, tubes, dalle de PCH & $\dot{Q} = \lambda S \Delta T / e$ \\
    Convection & Transport par mouvement du fluide & Radiateurs, échangeurs, CTA, VEC & $\dot{Q} = h S \Delta T$ \\
    Rayonnement & Ondes électromagnétiques (infrarouge) & Panneaux rayonnants, solaire passif, vitrages & $\dot{Q} = \varepsilon \sigma T^4 S$ \\
    \bottomrule
  \end{tabular}
\end{table}

% ------------------------------------------------------------
% Diagramme TikZ — Les 3 modes de transfert thermique
% ------------------------------------------------------------
\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      >=Stealth,
      font=\small,
      bloc/.style={rectangle, rounded corners=4pt, minimum width=2.8cm,
                   minimum height=1.0cm, align=center, text=white},
    ]

    % --- Conduction ---
    \node[bloc, fill=FedBlue]  (cond) at (0,0)
      {\textbf{Conduction}\\solide / fluide repos};
    \draw[->, FedBlue, line width=1.2pt]
      (-2.2,0) -- node[above, FedBlue]{\(\lambda\)} (-0.1,0);
    \node[above=0.55cm of cond, FedBlue]
      {\(\dot{Q}=\lambda S\Delta T / e\)};

    % --- Convection ---
    \node[bloc, fill=FedOrange] (conv) at (5,0)
      {\textbf{Convection}\\fluide en mouvement};
    % flèches de mouvement de fluide
    \foreach \y in {0.3, 0, -0.3} {
      \draw[->, FedOrange, line width=0.8pt]
        (3.0,\y) -- (3.8,\y);
    }
    \node[above=0.55cm of conv, FedOrange]
      {\(\dot{Q}=h\,S\Delta T\)};

    % --- Rayonnement ---
    \node[bloc, fill=FedRed]   (ray)  at (10,0)
      {\textbf{Rayonnement}\\ondes électromagnétiques};
    % zigzag symbolisant ondes IR
    \draw[FedRed, line width=1.2pt,
          decorate, decoration={snake, amplitude=2pt, segment length=6pt}]
      (7.9,0) -- (8.9,0);
    \node[above=0.55cm of ray, FedRed]
      {\(\dot{Q}=\varepsilon\sigma T^4 S\)};

    % Support matériel OUI/NON
    \node[below=0.6cm of cond, FedBlue, font=\footnotesize]
      {Support matériel~: \textbf{oui}};
    \node[below=0.6cm of conv, FedOrange, font=\footnotesize]
      {Support matériel~: \textbf{oui}};
    \node[below=0.6cm of ray, FedRed, font=\footnotesize]
      {Support matériel~: \textbf{non}};

  \end{tikzpicture}
  \caption{Les trois modes de transfert thermique et leurs formules clés}
  \label{fig:modes_transfert}
\end{figure}

% ============================================================
\section{Conduction thermique}
% ============================================================

\subsection{Loi de Fourier}

La conduction thermique est régie par la loi de Fourier. Pour une paroi plane en régime
permanent, le flux thermique surfacique \(\varphi\) (en \si{\watt\per\meter\squared}) est
proportionnel au gradient de température :

\begin{equation}
  \varphi = -\lambda \cdot \frac{\mathrm{d}T}{\mathrm{d}x}
  \label{eq:fourier_diff}
\end{equation}

Pour une paroi homogène d'épaisseur \(e\) (\si{\meter}), de conductivité thermique
\(\lambda\) (\si{\watt\per\meter\per\kelvin}), avec une différence de température
\(\Delta T = T_1 - T_2\) (\si{\kelvin}), la loi de Fourier s'écrit sous forme intégrée :

\begin{equation}
  \varphi = \lambda \cdot \frac{\Delta T}{e}
  \qquad \Rightarrow \qquad
  \dot{Q} = \lambda \cdot \frac{S \cdot \Delta T}{e}
  \label{eq:fourier_integre}
\end{equation}

\textbf{Variables :}
\begin{itemize}
  \item \(\lambda\) : conductivité thermique du matériau [\si{\watt\per\meter\per\kelvin}]
  \item \(e\) : épaisseur de la paroi [\si{\meter}]
  \item \(\Delta T\) : différence de température entre les deux faces [\si{\kelvin}] ou
        [\si{\degreeCelsius}]
  \item \(S\) : surface de la paroi [\si{\meter\squared}]
  \item \(\dot{Q}\) : flux thermique total [\si{\watt}]
\end{itemize}

\textbf{Valeurs de conductivité thermique courantes en GCF :}

\begin{table}[H]
  \centering
  \caption{Conductivités thermiques des matériaux courants en GCF}
  \begin{tabular}{lcc}
    \toprule
    Matériau & \(\lambda\) (\si{\watt\per\meter\per\kelvin}) & Usage typique \\
    \midrule
    Béton lourd & 1,75 & Structure portante \\
    Béton cellulaire (YTONG) & 0,11 & Paroi légère, bonne isolation \\
    Brique pleine & 0,84 & Maçonnerie traditionnelle \\
    Brique creuse & 0,45 & Paroi légère \\
    Bois massif & 0,15 & Ossature bois \\
    Contreplaqué & 0,13 & Panneaux de coffrage \\
    Laine de verre & 0,035–0,040 & Isolation thermique courante \\
    Laine de roche & 0,035–0,042 & Isolation thermique + acoustique \\
    Polystyrène expansé (PSE) & 0,030–0,038 & ITE, isolation sous dallage \\
    Polyuréthane (PUR) & 0,022–0,028 & Isolation haute performance \\
    Plâtre & 0,35 & Enduit intérieur \\
    Verre simple & 1,00 & Vitrage \\
    Acier & 50 & Tubes, structures \\
    Cuivre & 380 & Tubes, échangeurs \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Résistance thermique}

\begin{definition}
  La \textbf{résistance thermique} d'une couche de matériau (résistance surfacique, notée
  \(R\)) est le rapport de l'épaisseur sur la conductivité thermique :
  \begin{equation}
    R = \frac{e}{\lambda}
    \qquad \left[\si{\meter\squared\kelvin\per\watt}\right]
    \label{eq:resistance_thermique}
  \end{equation}
  Elle représente la capacité d'un matériau à s'opposer au passage de la chaleur : plus
  \(R\) est grand, plus l'isolation est efficace.
\end{definition}

Pour une paroi multicouche composée de \(n\) couches en série, les résistances s'additionnent :

\begin{equation}
  R_{tot} = R_{si} + \sum_{i=1}^{n} \frac{e_i}{\lambda_i} + R_{se}
  \label{eq:resistance_totale}
\end{equation}

avec :
\begin{itemize}
  \item \(R_{si}\) : résistance superficielle intérieure. Valeur standard : \SI{0,13}{\meter\squared\kelvin\per\watt}
  \item \(R_{se}\) : résistance superficielle extérieure. Valeur standard : \SI{0,04}{\meter\squared\kelvin\per\watt}
  \item \(e_i / \lambda_i\) : résistance de chaque couche de matériau
\end{itemize}

\begin{definition}
  Le \textbf{coefficient de transmission thermique} (ou transmittance) \(U\) est l'inverse
  de la résistance thermique totale :
  \begin{equation}
    U = \frac{1}{R_{tot}}
    \qquad \left[\si{\watt\per\meter\squared\per\kelvin}\right]
    \label{eq:coefficient_U}
  \end{equation}
  Il caractérise les déperditions thermiques à travers une paroi. La RE2020 fixe des valeurs
  maximales de \(U\) selon le type de paroi.
\end{definition}

Le flux thermique total traversant une paroi de surface \(S\) est alors :

\begin{equation}
  \dot{Q} = U \cdot S \cdot \Delta T = \frac{\Delta T}{R_{tot}} \cdot S
  \label{eq:flux_total}
\end{equation}

\begin{methode}[title=Calcul de la résistance thermique d'une paroi multicouche]
  \begin{enumerate}
    \item \textbf{Identifier} toutes les couches de la paroi (de l'intérieur vers
          l'extérieur) et noter leur épaisseur \(e_i\) et leur conductivité \(\lambda_i\).
    \item \textbf{Calculer} la résistance de chaque couche :
          \(R_i = e_i / \lambda_i\).
    \item \textbf{Ajouter} les résistances superficielles :
          \(R_{si} = \SI{0,13}{\meter\squared\kelvin\per\watt}\) (intérieur) et
          \(R_{se} = \SI{0,04}{\meter\squared\kelvin\per\watt}\) (extérieur).
    \item \textbf{Sommer} toutes les résistances pour obtenir \(R_{tot}\).
    \item \textbf{Calculer} le coefficient \(U = 1/R_{tot}\).
    \item \textbf{Vérifier} la conformité vis-à-vis de la réglementation (RE2020).
  \end{enumerate}
\end{methode}

% ------------------------------------------------------------
% Diagramme TikZ — Paroi multicouche et analogie électrique
% ------------------------------------------------------------
\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      >=Stealth, font=\small,
      couche/.style={rectangle, minimum width=1.4cm, minimum height=3.0cm,
                     align=center, draw=black, line width=0.6pt},
      res/.style={rectangle, minimum width=1.2cm, minimum height=0.55cm,
                  align=center, draw=FedBlue, fill=FedBlue!15,
                  line width=0.8pt, font=\footnotesize},
    ]

    % ---- Représentation physique de la paroi ----
    % Couche de convection intérieure (fond)
    \fill[FedOrange!20] (-1.0,-1.5) rectangle (0.0,1.5);
    \node[align=center, font=\footnotesize] at (-0.5,0)
      {\(h_i\)\\conv.\\int.};

    % Couche 1 — enduit plâtre
    \fill[gray!30] (0.0,-1.5) rectangle (0.8,1.5);
    \draw (0.0,-1.5) rectangle (0.8,1.5);
    \node[align=center, font=\footnotesize] at (0.4,0)
      {Pl\^{a}tre\\$e_1$};

    % Couche 2 — béton
    \fill[gray!60] (0.8,-1.5) rectangle (2.2,1.5);
    \draw (0.8,-1.5) rectangle (2.2,1.5);
    \node[align=center, font=\footnotesize] at (1.5,0)
      {Béton\\$e_2$};

    % Couche 3 — isolant (couleur verte)
    \fill[FedGreen!30] (2.2,-1.5) rectangle (3.6,1.5);
    \draw (2.2,-1.5) rectangle (3.6,1.5);
    \node[align=center, font=\footnotesize] at (2.9,0)
      {Isolant\\$e_3,\lambda_3$};

    % Couche 4 — enduit ext.
    \fill[gray!40] (3.6,-1.5) rectangle (4.2,1.5);
    \draw (3.6,-1.5) rectangle (4.2,1.5);
    \node[align=center, font=\footnotesize] at (3.9,0)
      {End.\\ext.};

    % Couche convection extérieure
    \fill[FedBlue!15] (4.2,-1.5) rectangle (5.2,1.5);
    \node[align=center, font=\footnotesize] at (4.7,0)
      {\(h_e\)\\conv.\\ext.};

    % Températures
    \node[left,  font=\small, FedOrange] at (-1.2, 0) {\(T_i\)};
    \node[right, font=\small, FedBlue]   at  (5.4, 0) {\(T_e\)};
    \draw[->, FedOrange, line width=1.2pt] (-1.6, 0) -- (-1.1, 0);

    % ---- Analogie électrique (en dessous) ----
    % Chaque résistance est dessinée avec coordonnées absolues
    % Chaîne : Ti --[Rsi]-- o --[R1]-- o --[R2]-- o --[R3]-- o --[R4]-- o --[Rse]-- Te
    % Positions x des bornes gauches : -1.2, -0.5, 1.0, 2.2, 3.6, 4.8 ; largeur boîte=0.8
    \node[font=\small, FedBlue] at (2.1,-2.2) {Analogie électrique : résistances en série};

    % Fil d'entrée
    \node[left, FedOrange, font=\small]  at (-1.4,-3.4) {\(T_i\)};
    \draw[FedOrange, line width=1.2pt] (-1.4,-3.4) -- (-0.9,-3.4);
    % Rsi
    \draw[FedOrange, fill=FedOrange!15, line width=0.8pt]
      (-0.9,-3.67) rectangle (-0.1,-3.13);
    \node[font=\footnotesize, FedOrange] at (-0.5,-3.4) {\(R_{si}\)};
    \draw[gray!70, line width=1.2pt] (-0.1,-3.4) -- (0.6,-3.4);
    % R1
    \draw[gray!70, fill=FedBlue!10, line width=0.8pt]
      (0.6,-3.67) rectangle (1.4,-3.13);
    \node[font=\footnotesize] at (1.0,-3.4) {\(R_1\)};
    \draw[gray!70, line width=1.2pt] (1.4,-3.4) -- (2.1,-3.4);
    % R2
    \draw[gray!70, fill=FedBlue!10, line width=0.8pt]
      (2.1,-3.67) rectangle (2.9,-3.13);
    \node[font=\footnotesize] at (2.5,-3.4) {\(R_2\)};
    \draw[gray!70, line width=1.2pt] (2.9,-3.4) -- (3.4,-3.4);
    % R3 (isolant — en vert, boîte plus large)
    \draw[FedGreen, fill=FedGreen!15, line width=0.8pt]
      (3.4,-3.67) rectangle (4.4,-3.13);
    \node[font=\footnotesize, FedGreen] at (3.9,-3.4) {\(R_3\)};
    \draw[gray!70, line width=1.2pt] (4.4,-3.4) -- (4.9,-3.4);
    % R4
    \draw[gray!70, fill=FedBlue!10, line width=0.8pt]
      (4.9,-3.67) rectangle (5.7,-3.13);
    \node[font=\footnotesize] at (5.3,-3.4) {\(R_4\)};
    \draw[FedBlue, line width=1.2pt] (5.7,-3.4) -- (6.2,-3.4);
    % Rse
    \draw[FedBlue, fill=FedBlue!15, line width=0.8pt]
      (6.2,-3.67) rectangle (7.0,-3.13);
    \node[font=\footnotesize, FedBlue] at (6.6,-3.4) {\(R_{se}\)};
    % Fil de sortie
    \draw[FedBlue, line width=1.2pt] (7.0,-3.4) -- (7.5,-3.4);
    \node[right, FedBlue, font=\small]  at (7.5,-3.4) {\(T_e\)};

  \end{tikzpicture}
  \caption{Paroi multicouche et analogie électrique des résistances thermiques en série
    (\(R_{tot}=R_{si}+R_1+R_2+R_3+R_4+R_{se}\))}
  \label{fig:paroi_multicouche}
\end{figure}

\subsection{Thermique des réseaux de distribution — flux linéique}

Pour les canalisations (tubes de chauffage, ECS), les déperditions sont caractérisées par le \textbf{flux linéique} :

\begin{equation}
  \varphi_L = \frac{\dot{Q}}{L} = U_L \cdot \Delta T \qquad \left[\si{\watt\per\metre}\right]
\end{equation}

avec \(U_L\) le coefficient de déperdition linéique [\si{\watt\per\metre\per\kelvin}], dépendant du diamètre du tube et de l'épaisseur d'isolant.

\begin{table}[H]
  \centering
  \caption{Déperditions linéiques typiques — tuyaux isolés en gaine technique (tuyau acier + isolant $\lambda = 0{,}04$\,W/m·K)}
  \begin{tabular}{lccc}
    \toprule
    DN tube & Épaisseur isolant & $U_L$ [\si{\watt\per\metre\per\kelvin}] & $\dot{Q}/L$ pour $\Delta T = 40$\,K \\
    \midrule
    DN 20 & 20 mm & 0,16 & 6,4 W/m \\
    DN 32 & 30 mm & 0,19 & 7,6 W/m \\
    DN 50 & 40 mm & 0,23 & 9,2 W/m \\
    DN 100 & 60 mm & 0,33 & 13,2 W/m \\
    \bottomrule
  \end{tabular}
\end{table}

% ============================================================
\section{Convection thermique}
% ============================================================

\subsection{Convection naturelle et forcée}

\begin{definition}
  La \textbf{convection naturelle} (ou libre) se produit lorsque les mouvements du fluide
  sont uniquement dus aux différences de masse volumique engendrées par les variations de
  température. Elle est responsable, par exemple, du panache de chaleur au-dessus d'un
  radiateur.
\end{definition}

\begin{definition}
  La \textbf{convection forcée} se produit lorsqu'un dispositif mécanique (pompe, ventilateur)
  impose le mouvement du fluide. Elle est caractéristique des ventilo-convecteurs, des
  échangeurs à plaques et des centrales de traitement d'air (CTA).
\end{definition}

La convection forcée est nettement plus efficace que la convection naturelle : les coefficients
d'échange sont en général \num{5} à \num{10} fois plus élevés.

\subsection{Loi de Newton — coefficient d'échange convectif}

Le flux thermique échangé par convection entre un fluide et une paroi est donné par la
\textbf{loi de Newton} :

\begin{equation}
  \dot{Q} = h \cdot S \cdot (T_{fluide} - T_{paroi})
  \label{eq:newton_convection}
\end{equation}

\textbf{Variables :}
\begin{itemize}
  \item \(h\) : coefficient d'échange convectif [\si{\watt\per\meter\squared\per\kelvin}]
  \item \(S\) : surface d'échange [\si{\meter\squared}]
  \item \(T_{fluide}\) : température du fluide en écoulement [\si{\degreeCelsius}]
  \item \(T_{paroi}\) : température de la surface [\si{\degreeCelsius}]
\end{itemize}

\textbf{Ordres de grandeur du coefficient \(h\) :}

\begin{table}[H]
  \centering
  \caption{Coefficients d'échange convectif selon la situation}
  \begin{tabular}{lc}
    \toprule
    Situation & \(h\) (\si{\watt\per\meter\squared\per\kelvin}) \\
    \midrule
    Convection naturelle dans l'air & 2–10 \\
    Convection forcée dans l'air (ventilation, CTA) & 20–100 \\
    Convection forcée dans l'eau (circuit hydraulique) & 500–10\,000 \\
    Ébullition de l'eau & 2\,000–50\,000 \\
    Condensation de vapeur d'eau & 5\,000–100\,000 \\
    \bottomrule
  \end{tabular}
\end{table}

Les résistances superficielles \(R_{si}\) et \(R_{se}\) de la norme NF EN ISO 6946 sont
liées aux coefficients d'échange convectif de surface :
\(R_{si} = 1/h_i = 1/7{,}7 \approx \SI{0,13}{\meter\squared\kelvin\per\watt}\) et
\(R_{se} = 1/h_e = 1/25 = \SI{0,04}{\meter\squared\kelvin\per\watt}\).

\subsection{Efficacité des échangeurs de chaleur}

Dans les installations CVC, les échangeurs à plaques sont omniprésents (production d'ECS, sous-stations réseau de chaleur, échangeur hydraulique PAC). On définit leur efficacité $\varepsilon$ :

\begin{equation}
  \varepsilon = \frac{\dot{Q}_{réel}}{\dot{Q}_{max}} = \frac{\dot{Q}_{réel}}{\dot{m}_{min} \cdot c_p \cdot (T_{chaud,e} - T_{froid,e})}
\end{equation}

\begin{attention}
  Un échangeur encrassé (tartre, biofilm) voit son coefficient d'échange $h$ diminuer fortement. Une augmentation de \SI{1}{\milli\metre} de tartre sur un échangeur cuivre réduit le flux de 20 à 30\,\%. La maintenance (détartrage annuel pour ECS) est essentielle.
\end{attention}

% ============================================================
\section{Rayonnement thermique}
% ============================================================

\subsection{Loi de Stefan-Boltzmann}

Tout corps à une température supérieure au zéro absolu émet un rayonnement électromagnétique.
Pour un corps noir parfait, la puissance rayonnée par unité de surface est donnée par la
\textbf{loi de Stefan-Boltzmann} :

\begin{equation}
  M^{\circ} = \sigma \cdot T^4
  \qquad \left[\si{\watt\per\meter\squared}\right]
  \label{eq:stefan_boltzmann}
\end{equation}

avec \(\sigma = \SI{5,67e-8}{\watt\per\meter\squared\per\kelvin\tothe{4}}\) la constante
de Stefan-Boltzmann et \(T\) la température en \si{\kelvin}.

\begin{definition}
  \textbf{Corps noir} : corps idéal qui absorbe l'intégralité du rayonnement incident, quelle
  que soit la longueur d'onde et l'angle d'incidence. Il constitue la référence en matière de
  rayonnement thermique.
\end{definition}

\begin{definition}
  \textbf{Émissivité} (\(\varepsilon\)) : rapport de l'émittance d'un corps réel à celle du
  corps noir à la même température. Adimensionnel, compris entre 0 et 1.
  Pour un corps réel (corps gris) : \(M = \varepsilon \cdot \sigma \cdot T^4\).
\end{definition}

\textbf{Valeurs d'émissivité courantes :}
\begin{table}[H]
  \centering
  \caption{Émissivités des matériaux courants}
  \begin{tabular}{lc}
    \toprule
    Matériau / Surface & Émissivité \(\varepsilon\) \\
    \midrule
    Corps noir (référence) & 1,00 \\
    Peinture blanche mate & 0,90–0,95 \\
    Béton, plâtre & 0,85–0,95 \\
    Verre & 0,84–0,90 \\
    Bois vernis & 0,80–0,90 \\
    Aluminium anodisé & 0,77–0,84 \\
    Aluminium poli (film de pare-vapeur) & 0,04–0,06 \\
    Acier inoxydable poli & 0,10–0,20 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{attention}
  La loi de Stefan-Boltzmann utilise la \textbf{température absolue en kelvin}, jamais en
  degré Celsius. Une erreur de conversion entraîne des résultats faux d'un facteur très
  important. Vérifier systématiquement : \(T\,[\si{\kelvin}] = \theta\,[\si{\degreeCelsius}]
  + 273{,}15\).
\end{attention}

\subsection{Rayonnement en pratique CVC}

En génie climatique, le rayonnement intervient notamment dans :
\begin{itemize}
  \item les \textbf{panneaux rayonnants} et planchers chauffants (part de rayonnement dans
        le confort thermique) ;
  \item les \textbf{apports solaires} à travers les vitrages (pris en compte dans les bilans
        d'été, RE2020) ;
  \item les \textbf{surfaces basse émissivité} des vitrages doubles ou triples, qui réduisent
        le rayonnement vers l'extérieur.
\end{itemize}

Le facteur solaire \(g\) d'un vitrage caractérise la fraction du rayonnement solaire transmise à l'intérieur :
\begin{itemize}
  \item Vitrage simple : $g \approx 0{,}85$
  \item Double vitrage standard : $g \approx 0{,}60$
  \item Double vitrage à contrôle solaire : $g \approx 0{,}30$–$0{,}40$ (recommandé façades sud RE2020)
\end{itemize}

% ============================================================
\section{Bilan thermique et enthalpie}
% ============================================================

\subsection{Premier principe de la thermodynamique — système ouvert}

En régime permanent, pour un système ouvert (fluide en circulation continue), le premier
principe s'écrit :

\begin{equation}
  \dot{Q} - \dot{W} = \dot{m} \cdot (h_{sortie} - h_{entrée})
  \label{eq:premier_principe_ouvert}
\end{equation}

En l'absence de travail mécanique (\(\dot{W} = 0\), hypothèse valable pour les circuits
hydrauliques sans pompage à l'intérieur du volume de contrôle) :

\begin{equation}
  \dot{Q} = \dot{m} \cdot (h_{sortie} - h_{entrée})
  \label{eq:bilan_enthalpique}
\end{equation}

\subsection{Enthalpie spécifique}

\begin{definition}
  L'\textbf{enthalpie spécifique} \(h\) est une fonction d'état définie par :
  \begin{equation}
    h = u + P \cdot v \qquad \left[\si{\joule\per\kilogram}\right]
    \label{eq:enthalpie}
  \end{equation}
  où \(u\) est l'énergie interne massique, \(P\) la pression et \(v\) le volume massique.
  Pour un liquide ou un gaz parfait, la variation d'enthalpie à pression constante s'écrit :
  \(\Delta h = c_p \cdot \Delta T\).
\end{definition}

\subsection{Puissance thermique d'un fluide caloporteur}

Pour un circuit de chauffage ou de climatisation où un fluide (eau, air) transporte de
l'énergie thermique, la puissance échangée est :

\begin{equation}
  \boxed{
    \dot{Q} = \dot{m} \cdot c_p \cdot \Delta T
  }
  \label{eq:puissance_fluide}
\end{equation}

\textbf{Variables et unités :}
\begin{itemize}
  \item \(\dot{Q}\) : puissance thermique [\si{\watt}] ou [\si{\kilo\watt}]
  \item \(\dot{m}\) : débit massique du fluide [\si{\kilogram\per\second}] ou
        [\si{\kilogram\per\hour}]
  \item \(c_p\) : capacité thermique massique du fluide [\si{\joule\per\kilogram\per\kelvin}]
        \\ (eau liquide : \SI{4186}{\joule\per\kilogram\per\kelvin} ;
           air : \SI{1006}{\joule\per\kilogram\per\kelvin})
  \item \(\Delta T = T_{sortie} - T_{entrée}\) : écart de température [\si{\kelvin}] ou
        [\si{\degreeCelsius}]
\end{itemize}

\begin{attention}
  Le débit volumique \(\dot{V}\) [\si{\liter\per\hour} ou \si{\meter\cubed\per\hour}] est
  lié au débit massique par : \(\dot{m} = \rho \cdot \dot{V}\), avec \(\rho\) la masse
  volumique du fluide. Pour l'eau à \SI{60}{\degreeCelsius} : \(\rho \approx
  \SI{983}{\kilogram\per\meter\cubed}\). On utilise souvent l'approximation
  \(\rho \approx \SI{1000}{\kilogram\per\meter\cubed}\) pour les calculs courants.
\end{attention}

La formule pratique avec le débit volumique en \si{\liter\per\hour} et \(c_p\) de l'eau :

\begin{equation}
  \dot{Q}\,[\si{\watt}] = \dot{V}\,[\si{\liter\per\hour}] \times 1{,}163 \times \Delta T\,[\si{\kelvin}]
  \label{eq:formule_pratique_eau}
\end{equation}

Le facteur \num{1,163} est le produit \((\rho \cdot c_p) / 3600\) pour l'eau
\(= 1000 \times 4186 / 3\,600\,000 \approx \num{1,163}\,\si{\watt\hour\per\liter\per\kelvin}\).

\subsection{Coefficient de déperdition thermique d'un bâtiment}

Le coefficient de déperdition thermique global $H_T$ (en \si{\watt\per\kelvin}) est la somme des contributions de toutes les parois :

\begin{equation}
  H_T = \sum_i U_i \cdot S_i + \sum_j \Psi_j \cdot L_j + \sum_k \chi_k
\end{equation}

\begin{itemize}
  \item $U_i \cdot S_i$ : déperdition surfacique paroi $i$ [\si{\watt\per\kelvin}]
  \item $\Psi_j \cdot L_j$ : ponts thermiques linéaires (acrotère, menuiseries, planchers…) [\si{\watt\per\kelvin}]
  \item $\chi_k$ : ponts thermiques ponctuels (vis, chevilles métalliques) [\si{\watt\per\kelvin}]
\end{itemize}

La puissance de déperdition à la température de base est alors :
\begin{equation}
  \dot{Q}_{dep} = H_T \cdot (T_{int} - T_{base})
\end{equation}

% ============================================================
\section{Hygrothermie — condensation en paroi}
% ============================================================

\subsection{Point de rosée et condensation}

\begin{definition}
  Le \textbf{point de rosée} $T_{rosée}$ est la température à laquelle l'air humide atteint la saturation en vapeur d'eau. En dessous de cette température, la vapeur se condense en eau liquide.
\end{definition}

\begin{table}[H]
  \centering
  \caption{Points de rosée selon la température et l'humidité relative de l'air}
  \begin{tabular}{lccc}
    \toprule
    $T_{air}$ (°C) & HR = 40\,\% & HR = 60\,\% & HR = 80\,\% \\
    \midrule
    20 & 6,1 °C & 12,0 °C & 16,4 °C \\
    18 & 4,3 °C & 10,1 °C & 14,5 °C \\
    15 & 1,5 °C & 7,2 °C & 11,5 °C \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Condensation superficielle et en paroi (interstitielle)}

\begin{itemize}
  \item \textbf{Condensation superficielle} : se produit sur les surfaces froides (simple vitrage, pont thermique). Risque de moisissures. Évitée en augmentant $T_{paroi}$ (isolation) ou en réduisant $HR$ (ventilation).
  \item \textbf{Condensation interstitielle} : vapeur diffuse à travers la paroi et se condense à l'intérieur (entre isolant et support). Détectée par la méthode de Glaser (profil de température vs profil de pression de saturation).
\end{itemize}

\begin{methode}[title=Prévention de la condensation interstitielle]
  \begin{enumerate}
    \item Placer le \textbf{pare-vapeur côté chaud} (côté intérieur en hiver) : film polyéthylène ou OSB 3.
    \item Éviter les isolants hygroscopiques sans protection.
    \item Assurer la continuité du pare-vapeur (joints, emboîtements).
    \item Ventiler la lame d'air éventuelle (façade ventilée).
  \end{enumerate}
\end{methode}

% ============================================================
\section{Exemples résolus}
% ============================================================

\begin{exemple}[title=Calcul de perte thermique par une paroi multicouche]

\textbf{Énoncé :} Un mur de façade d'un immeuble de bureau est composé (de l'intérieur vers
l'extérieur) des couches suivantes :
\begin{enumerate}
  \item Enduit plâtre intérieur : \(e_1 = \SI{1,5}{\centi\meter}\), \(\lambda_1 = \SI{0,35}{\watt\per\meter\per\kelvin}\)
  \item Béton armé : \(e_2 = \SI{20}{\centi\meter}\), \(\lambda_2 = \SI{1,75}{\watt\per\meter\per\kelvin}\)
  \item Laine de verre (panneaux semi-rigides) : \(e_3 = \SI{10}{\centi\meter}\), \(\lambda_3 = \SI{0,04}{\watt\per\meter\per\kelvin}\)
  \item Enduit ciment extérieur : \(e_4 = \SI{2}{\centi\meter}\), \(\lambda_4 = \SI{1,15}{\watt\per\meter\per\kelvin}\)
\end{enumerate}

\textit{Données :} \(R_{si} = \SI{0,13}{\meter\squared\kelvin\per\watt}\),
\(R_{se} = \SI{0,04}{\meter\squared\kelvin\per\watt}\), $\theta_i = \SI{20}{\degreeCelsius}$, $\theta_e = \SI{0}{\degreeCelsius}$, surface du mur $S = \SI{50}{\meter\squared}$.

\textbf{Résolution :}

\textit{Étape 1 — Résistance de chaque couche}
\[
  R_1 = \frac{0{,}015}{0{,}35} = \SI{0,043}{\meter\squared\kelvin\per\watt}
\]
\[
  R_2 = \frac{0{,}20}{1{,}75} = \SI{0,114}{\meter\squared\kelvin\per\watt}
\]
\[
  R_3 = \frac{0{,}10}{0{,}04} = \SI{2,500}{\meter\squared\kelvin\per\watt}
\]
\[
  R_4 = \frac{0{,}02}{1{,}15} = \SI{0,017}{\meter\squared\kelvin\per\watt}
\]

\textit{Étape 2 — Résistance thermique totale (NF EN ISO 6946)}
\[
  R_{tot} = 0{,}13 + 0{,}043 + 0{,}114 + 2{,}500 + 0{,}017 + 0{,}04
  = \boxed{\SI{2,844}{\meter\squared\kelvin\per\watt}}
\]

\textit{Étape 3 — Coefficient U}
\[
  U = \frac{1}{2{,}844} = \SI{0,352}{\watt\per\meter\squared\per\kelvin}
\]

\textit{Étape 4 — Flux thermique total}
\[
  \dot{Q} = U \cdot S \cdot \Delta T = 0{,}352 \times 50 \times 20 = \boxed{\SI{352}{\watt}}
\]

\textbf{Conclusion :} $U = \SI{0,352}{\watt\per\meter\squared\per\kelvin} < U_{max,RE2020} = \SI{0,36}{\watt\per\meter\squared\per\kelvin}$ — conforme.
\end{exemple}

\begin{exemple}[title=Calcul de puissance d'un radiateur eau chaude]

\textbf{Énoncé :} Un radiateur à eau chaude est alimenté avec un débit volumique de
\SI{200}{\liter\per\hour}. La température d'entrée est $\theta_{entrée} = \SI{70}{\degreeCelsius}$
et la température de sortie est $\theta_{sortie} = \SI{55}{\degreeCelsius}$. Calculer la
puissance thermique émise par ce radiateur.

\textbf{Méthode pratique (facteur 1,163) :}
\[
  \dot{Q} = \dot{V} \times 1{,}163 \times \Delta T = 200 \times 1{,}163 \times (70-55)
  = 200 \times 1{,}163 \times 15 = \boxed{\SI{3489}{\watt} \approx \SI{3{,}5}{\kilo\watt}}
\]

\textbf{Vérification débit massique :}
\[
  \dot{m} = \rho \cdot \dot{V} = 980 \times \frac{200}{3600} = \SI{54{,}4}{\kilogram\per\hour}
  = \SI{0{,}01511}{\kilogram\per\second}
\]
\[
  \dot{Q} = 0{,}01511 \times 4186 \times 15 = \SI{3421}{\watt}
\]
Écart de 2\,\% par rapport à la méthode approchée — acceptable en pratique.
\end{exemple}

\begin{exemple}[title=Bilan thermique d'une pièce de bureau — puissance de chauffage]

\textbf{Énoncé :} Un bureau de \SI{20}{\meter\squared} situé en zone H1 ($T_{base} = -7$\,°C). Parois : mur ext. (\SI{12}{\meter\squared}, $U = \SI{0,30}{\watt\per\meter\squared\per\kelvin}$), fenêtre DV (\SI{4}{\meter\squared}, $U = \SI{1,40}{\watt\per\meter\squared\per\kelvin}$), plancher haut (\SI{20}{\meter\squared}, $U = \SI{0,25}{\watt\per\meter\squared\per\kelvin}$), ponts thermiques $\Psi L = \SI{0,45}{\watt\per\kelvin}$.
Apports internes : éclairage 200\,W, 2 occupants × 80\,W, informatique 150\,W.

\textit{Étape 1 — Coefficient de déperdition $H_T$ :}
\[
  H_T = (0{,}30 \times 12) + (1{,}40 \times 4) + (0{,}25 \times 20) + 0{,}45
  = 3{,}60 + 5{,}60 + 5{,}00 + 0{,}45 = \SI{14{,}65}{\watt\per\kelvin}
\]

\textit{Étape 2 — Déperditions à $T_{base}$ :}
\[
  \dot{Q}_{dep} = 14{,}65 \times (20 - (-7)) = 14{,}65 \times 27 = \SI{395{,}6}{\watt}
\]

\textit{Étape 3 — Apports internes :}
\[
  \dot{Q}_{int} = 200 + 160 + 150 = \SI{510}{\watt}
\]

\textit{Étape 4 — Puissance de chauffage à installer :}

En période occupée, $\dot{Q}_{int} > \dot{Q}_{dep}$ → pas de chauffage nécessaire (tertiaire dense).
En période inoccupée (nuit, apports nuls) : $\dot{Q}_{chauf} = \SI{396}{\watt}$. Prévoir un émetteur d'appoint d'au moins \SI{400}{\watt}.
\end{exemple}

\begin{exemple}[title=Vérification du risque de condensation superficielle]

\textbf{Énoncé :} Un simple vitrage ($U = \SI{5{,}8}{\watt\per\meter\squared\per\kelvin}$) est exposé à l'extérieur à $T_e = \SI{5}{\degreeCelsius}$. La pièce est à $T_i = \SI{20}{\degreeCelsius}$, $HR = 60\,\%$ (point de rosée = 12\,°C). Vérifier s'il y a risque de condensation.

\textbf{Température de surface intérieure du vitrage :}

Par résistances en série, la fraction de $\Delta T$ chutant côté intérieur ($R_{si} = 0{,}13$\,m²K/W) :
\[
  R_{tot} = R_{si} + \frac{1}{U_{vitrage}} = 0{,}13 + \frac{1}{5{,}8} = 0{,}13 + 0{,}172 = \SI{0{,}302}{\meter\squared\kelvin\per\watt}
\]
\[
  T_{surface} = T_i - \frac{R_{si}}{R_{tot}} \cdot (T_i - T_e) = 20 - \frac{0{,}13}{0{,}302} \times 15 = 20 - 6{,}5 = \SI{13{,}5}{\celsius}
\]

$T_{surface} = 13{,}5$\,°C $>$ $T_{rosée} = 12$\,°C → pas de condensation, mais marge très faible. En cas de montée de HR à 70\,\% ($T_{rosée} \approx 14{,}4$\,°C), condensation assurée. Remplacement par DV recommandé.
\end{exemple}

% ============================================================
\section{Éléments de réglementation}
% ============================================================

\subsection{RE2020 — Réglementation Environnementale 2020}

Entrée en vigueur le 1\textsuperscript{er} janvier 2022 pour les maisons individuelles et les
logements collectifs neufs, et progressivement étendue aux bâtiments tertiaires, la RE2020
remplace la RT2012. Ses trois piliers sont :
\begin{enumerate}
  \item \textbf{Efficacité énergétique} : réduction de la consommation d'énergie primaire
        (indicateur \(Ep_{max}\)).
  \item \textbf{Confort d'été} : limitation de l'inconfort en période chaude (indicateur DH,
        degrés-heures d'inconfort).
  \item \textbf{Impact carbone} : prise en compte de l'analyse du cycle de vie (ACV) du
        bâtiment, indicateur \(IC_{bât}\).
\end{enumerate}

\textbf{Exigences sur les parois opaques (résidentiels, zone H1) :}

\begin{table}[H]
  \centering
  \caption{Valeurs maximales de $U$ selon la RE2020 et la RT2012}
  \begin{tabular}{lcc}
    \toprule
    Type de paroi & \(U_{max}\) RE2020 & \(U_{max}\) RT2012 \\
                  & [\si{\watt\per\meter\squared\per\kelvin}] & [\si{\watt\per\meter\squared\per\kelvin}] \\
    \midrule
    Murs donnant sur l'extérieur & 0,36 & 0,40 \\
    Plancher bas sur vide sanitaire & 0,27 & 0,36 \\
    Toiture-terrasse & 0,20 & 0,25 \\
    Comble aménagé & 0,20 & 0,22 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{DTU 45.4 — Isolation thermique des bâtiments}

Le DTU 45.4 définit les règles de mise en œuvre des isolants thermiques dans les bâtiments.
Il précise notamment :
\begin{itemize}
  \item les conditions de mise en œuvre (fixation, joints, continuité de l'isolant) ;
  \item la protection contre l'humidité (pare-vapeur côté chaud en hiver) ;
  \item les incompatibilités entre matériaux ;
  \item les règles de sécurité incendie pour les isolants.
\end{itemize}

\subsection{NF EN ISO 6946 — Résistance thermique des parois}

Cette norme internationale définit la méthode de calcul de la résistance thermique et du
coefficient de transmission thermique des parois de bâtiment composées de couches
thermiquement homogènes. Elle fixe notamment les valeurs des résistances superficielles
\(R_{si}\) et \(R_{se}\) selon la direction du flux et la position de la paroi.

\begin{table}[H]
  \centering
  \caption{Résistances superficielles selon NF EN ISO 6946}
  \begin{tabular}{lccc}
    \toprule
    Direction du flux & Position & \(R_{si}\) [\si{\meter\squared\kelvin\per\watt}]
                      & \(R_{se}\) [\si{\meter\squared\kelvin\per\watt}] \\
    \midrule
    Ascendant (plancher chauffé par le bas) & Plancher chauffant & 0,10 & 0,13 \\
    Horizontal (paroi verticale) & Mur & 0,13 & 0,04 \\
    Descendant (chauffé par le haut) & Toiture & 0,17 & 0,04 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{description}
  \item[NF EN 12831] Méthode de calcul des besoins en chaleur — déperditions thermiques pour le dimensionnement des générateurs (température de base, ponts thermiques, renouvellement d'air).
  \item[NF EN ISO 10211] Ponts thermiques dans les bâtiments — calcul des flux thermiques et des températures de surface. Utilisée pour déterminer les coefficients linéiques $\Psi$.
  \item[NF EN 13829 / ISO 9972] Mesure de la perméabilité à l'air des bâtiments — essai de pressurisation (porte Blower Door). Obligatoire en RE2020.
\end{description}

% ------------------------------------------------------------
% Diagramme TikZ — Courbe de chauffe de l'eau (chaleur sensible + latente)
% ------------------------------------------------------------
\begin{figure}[H]
  \centering
  \begin{tikzpicture}[>=Stealth, font=\small]

    % Axes
    \draw[->, line width=1.0pt] (0,0) -- (10.5,0)
      node[right] {Énergie apportée \(Q\) [\si{\kilo\joule}]};
    \draw[->, line width=1.0pt] (0,0) -- (0,5.5)
      node[above] {Température \(T\) [\si{\degreeCelsius}]};

    % Graduations T
    \foreach \y/\lab in {0.5/-50, 1.0/0, 3.5/100, 5.0/200} {
      \draw (0,\y) -- (-0.1,\y) node[left, font=\footnotesize] {\lab};
    }
    % Graduations Q fictives (relatives)
    \foreach \x/\xlab in {1.0/0, 2.5/209, 5.0/250, 7.5/669, 8.5/2{,}256, 9.5/2{,}500} {
      \draw (\x,0) -- (\x,-0.15) node[below, font=\tiny, rotate=35, anchor=east] {};
    }

    % Zone 1 : glace — chaleur sensible (T: -50 → 0 °C)
    \fill[FedBlue!15] (1.0,0) -- (2.5,1.0) -- (2.5,0) -- cycle;
    \draw[FedBlue, line width=1.8pt] (1.0,0.5) -- (2.5,1.0);
    \node[FedBlue, font=\footnotesize, align=center] at (1.75,0.5)
      {Glace\\$c_p=2{,}09$};

    % Zone 2 : fusion (palier 0 °C)
    \fill[FedGreen!15] (2.5,1.0) rectangle (5.0,1.2);
    \draw[FedGreen, line width=1.8pt] (2.5,1.0) -- (5.0,1.0);
    \node[FedGreen, font=\footnotesize, align=center] at (3.75,1.35)
      {Fusion : $L_f=\SI{334}{\kilo\joule\per\kilogram}$};
    \draw[<->, FedGreen, dashed] (2.5,1.7) -- (5.0,1.7)
      node[midway, above, font=\footnotesize] {};

    % Zone 3 : eau liquide — chaleur sensible (T: 0 → 100 °C)
    \fill[FedOrange!15] (5.0,0.9) -- (7.5,3.5) -- (7.5,0) -- (5.0,0) -- cycle;
    \draw[FedOrange, line width=1.8pt] (5.0,1.0) -- (7.5,3.5);
    \node[FedOrange, font=\footnotesize, align=center] at (6.25,1.8)
      {Eau liquide\\$c_p=4{,}18$};

    % Zone 4 : vaporisation (palier 100 °C)
    \fill[FedRed!15] (7.5,3.5) rectangle (8.5,3.7);
    \draw[FedRed, line width=1.8pt] (7.5,3.5) -- (8.5,3.5);
    \node[FedRed, font=\footnotesize, align=center] at (8.0,3.9)
      {Vaporisation\\$L_v=\SI{2256}{\kilo\joule\per\kilogram}$};
    \draw[<->, FedRed, dashed] (7.5,4.3) -- (8.5,4.3)
      node[midway, above, font=\tiny] {};

    % Zone 5 : vapeur surchauffée
    \draw[FedRed!70!black, line width=1.8pt, dashed] (8.5,3.5) -- (9.8,4.5);
    \node[FedRed!70!black, font=\footnotesize, align=center] at (9.4,4.0)
      {Vapeur};

    % Annotations des paliers
    \draw[dashed, gray] (2.5,1.0) -- (2.5,0) node[below, font=\footnotesize] {0\,°C};
    \draw[dashed, gray] (7.5,3.5) -- (7.5,0) node[below, font=\footnotesize] {100\,°C};

    % Accolades chaleur sensible vs latente
    \node[FedBlue,  font=\footnotesize, right] at (0.2, 4.8)
      {\textbf{Chaleur sensible} : \(\Delta T \neq 0\), état inchangé};
    \node[FedGreen, font=\footnotesize, right] at (0.2, 4.3)
      {\textbf{Chaleur latente} : \(\Delta T = 0\), changement d'état};

  \end{tikzpicture}
  \caption{Courbe de chauffe de l'eau (1\,kg, pression atmosphérique) — alternance de chaleur
    sensible (\(\Delta T \neq 0\)) et de chaleur latente (paliers de changement d'état)}
  \label{fig:courbe_chauffe}
\end{figure}

\begin{tcolorbox}[
  enhanced, colback=FedBlue!3, colframe=FedBlue!50,
  fonttitle=\bfseries\sffamily, coltitle=FedBlue,
  title={FICHE MÉMO — Bases de la thermique}, boxrule=0.8pt, arc=2pt,
  width=\textwidth
]
\begin{multicols}{2}
\textbf{Chaleur sensible}
\[ Q = m \cdot c_p \cdot \Delta T \]
avec $m$ = masse [\si{\kilogram}], $c_p$ = capacité thermique massique [\si{\joule\per\kilogram\per\kelvin}], $\Delta T$ = écart de température [\si{\kelvin}]

\medskip
\textbf{Puissance thermique (fluide en circulation)}
\[ \dot{Q} = \dot{m} \cdot c_p \cdot \Delta T \]
avec $\dot{m}$ = débit massique [\si{\kilogram\per\second}]

\medskip
\textbf{Formule pratique eau liquide}
\[ \dot{Q}\,[\si{\watt}] = \dot{V}\,[\si{\liter\per\hour}] \times 1{,}163 \times \Delta T\,[\si{\kelvin}] \]

\medskip
\textbf{Conduction (loi de Fourier)}
\[ \dot{Q} = \lambda \cdot \frac{S \cdot \Delta T}{e} \]
avec $\lambda$ = conductivité thermique [\si{\watt\per\metre\per\kelvin}], $e$ = épaisseur [\si{\metre}]

\columnbreak

\textbf{Résistance thermique d'une couche}
\[ R = \frac{e}{\lambda} \quad \left[\si{\metre\squared\kelvin\per\watt}\right] \]

\medskip
\textbf{Coefficient de transmission surfacique}
\[ U = \frac{1}{\displaystyle R_{si} + \sum_i \frac{e_i}{\lambda_i} + R_{se}} \quad \left[\si{\watt\per\metre\squared\per\kelvin}\right] \]

\medskip
\textbf{Flux thermique total paroi}
\[ \dot{Q} = U \cdot S \cdot \Delta T \]

\medskip
\textbf{Convection (loi de Newton)}
\[ \dot{Q} = h \cdot S \cdot \Delta T \]
avec $h$ = coefficient d'échange convectif [\si{\watt\per\metre\squared\per\kelvin}]

\medskip
\textbf{Rayonnement (Stefan-Boltzmann)}
\[ M^{\circ} = \varepsilon \cdot \sigma \cdot T^4 \quad \left[\si{\watt\per\metre\squared}\right] \]
avec $\sigma = \SI{5{,}67e-8}{\watt\per\metre\squared\per\kelvin\tothe{4}}$, $T$ en \si{\kelvin}
\end{multicols}
\end{tcolorbox}

\finchapitre
